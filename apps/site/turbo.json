{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/apps/desktop/src/**",
        "$TURBO_ROOT$/apps/desktop/assets/skin/**",
        "$TURBO_ROOT$/packages/spotify/**"
      ]
    }
  }
}
